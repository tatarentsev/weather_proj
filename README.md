# weather proj API
Weather forecast project. I used two API requests (one for today's weather), another for 5-day forecasts (the rest are paid), Bootstrap for styling and importing icons, hooks, and Sass preprocessor.
